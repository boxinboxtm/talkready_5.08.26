# Copy this file to .env and fill in your key (get one at https://console.anthropic.com/).
# .env is git-ignored — the key never gets committed or shipped to the browser.
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional. Defaults to claude-sonnet-5 if omitted.
ANTHROPIC_MODEL=claude-sonnet-5
