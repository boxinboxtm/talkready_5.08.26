// Кто играет собеседника.
//
//   "local"  — сценарный движок в браузере. Ключи и интернет не нужны.
//              Реплики собраны заранее и подбираются по тому, что сказал пользователь.
//
//   "claude" — настоящая языковая модель через свой серверный прокси /api/chat.
//              Нужен ANTHROPIC_API_KEY в .env и запущенный `npm run dev:api`.
//
// Переключается одной строкой — остальной код не меняется:
// у обоих движков одинаковый интерфейс (askPartner / askFeedback).
export const ENGINE = "local";

// Задержка «собеседник думает», мс. У живой модели пауза возникает сама,
// локальному движку её нужно сымитировать, иначе ответ прилетает неестественно быстро.
export const LOCAL_THINKING_MS = [600, 1100];
